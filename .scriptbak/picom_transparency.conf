opacity-rule = [
   "80:class_g = 'Alacritty'",
];

# Blur 
blur:
{
    method = "dual_kawase";
    strength = 3;
}

# GLX Backend
backend = "glx";
glx-no-stencil = true;
glx-copy-from-front = false;
use-damage = true
glx-no-rebind-pixmap = true;


